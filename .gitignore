build/**
*.smc
*.sfc
oldies

# a816 build artifacts
a.out
a.out.adbg
*.o

# Generated build assets (keep intro source files)
assets/*
!assets/intro.col
!assets/intro.map
!assets/intro.set

.DS_Store
*.kss
!tests/savestates/*.kss

# Pixelmatch test artifacts
*.actual.png
*.diff.png

# Stashed previous goldens from UPDATE_GOLDENS=1 (kept locally for review)
*.prev.png
*.prev.bin
tests/goldens/index.html

# kintsuki project: the annotations are ours to keep, the traces are not.
# map.bin is a 2 MB byte-class bitmap and dma_log.tsv a capture log; both
# are regenerated by tracing the ROM.
ff4.kintsuki/map.bin
ff4.kintsuki/dma_log.tsv

# editable-install metadata
*.egg-info/
